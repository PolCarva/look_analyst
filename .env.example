# Configuración de la API de Análisis de Prendas

# API Key de Google Gemini (requerido)
GEMINI_API_KEY=tu_api_key_aqui

# Puerto del servidor (opcional, por defecto 3000)
PORT=3000

# Tamaño máximo de archivo en bytes (opcional, por defecto 8MB)
MAX_FILE_SIZE=8388608

# Directorio para archivos temporales (opcional, por defecto uploads)
UPLOAD_DIR=uploads

# URL base del frontend (opcional, por defecto http://localhost:PORT)
# Para producción, cambiar por tu dominio:
# FRONTEND_URL=https://tu-dominio.com
# Para desarrollo local:
FRONTEND_URL=http://localhost:3000
